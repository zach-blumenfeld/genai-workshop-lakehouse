# Claude Code auth: leave blank if signed in; otherwise use the workshop key or your own
ANTHROPIC_API_KEY=

# Neo4j sandbox - paste the credentials from the workshop
NEO4J_URI=
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=
NEO4J_DATABASE=neo4j

# BigQuery warehouse - read-only; the workshop's AutoFix dataset is pre-wired
GCP_PROJECT_ID=neo4jeventdemos
BIGQUERY_DATASET_ID=autofix_service
# BigQuery auth for the Python client: the workshop provides a read-only
# service-account key - set GOOGLE_APPLICATION_CREDENTIALS to its path - or run
# `gcloud auth application-default login` with your own GCP project.
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/read-only-sa-key.json

# Mock parts API
PARTS_API_URL=http://localhost:8800
PARTS_API_KEY=autofix-workshop-key

# Future: swap the warehouse connector to Databricks (keeps the shapes)
# WAREHOUSE_SOURCE=databricks
# DATABRICKS_SERVER_HOSTNAME=
# DATABRICKS_HTTP_PATH=
# DATABRICKS_TOKEN=
